-- بارگذاری کتابخانه به صورت محلی
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

-- تعریف پنجره اصلی
local Window = WindUI:CreateWindow({
    Title  = "Vitex Hub",
    Icon   = "crosshair",
    Author = "by Vitex",
    Theme  = "Violet",
    Acrylic = true,
})

-- تعریف تب PVP
local PvpTab = Window:Tab({
    Title = "PVP",
    Icon = "swords",
})

-- تعریف متغیرهای محلی برای سیستم ایم‌بات
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local IsAimbotEnabled = false

-- تابع پیدا کردن نزدیک‌ترین پلیر
local function GetClosestPlayer()
    local Target = nil
    local Distance = math.huge
    
    local MyCharacter = LocalPlayer.Character
    local MyPart = MyCharacter and MyCharacter:FindFirstChild("HumanoidRootPart")
    
    if not MyPart then return nil end

    for _, Player in pairs(Players:GetPlayers()) do
        if Player ~= LocalPlayer and Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
            local Humanoid = Player.Character:FindFirstChild("Humanoid")
            if Humanoid and Humanoid.Health > 0 then
                local Magnitude = (Player.Character.HumanoidRootPart.Position - MyPart.Position).Magnitude
                if Magnitude < Distance then
                    Distance = Magnitude
                    Target = Player.Character
                end
            end
        end
    end
    return Target
end

-- هوک کردن متامتد برای ایم‌بات (Silent Aim)
-- این بخش باعث میشه تیرها بدون نیاز به چرخاندن دوربین به هدف بخورند
local RawMetatable = getrawmetatable(game)
local OldIndex = RawMetatable.__index
setreadonly(RawMetatable, false)

RawMetatable.__index = newcclosure(function(Self, Key)
    if IsAimbotEnabled and Self == Mouse and (Key == "Hit" or Key == "Target") then
        local TargetCharacter = GetClosestPlayer()
        if TargetCharacter and TargetCharacter:FindFirstChild("HumanoidRootPart") then
            if Key == "Hit" then
                return TargetCharacter.HumanoidRootPart.CFrame
            elseif Key == "Target" then
                return TargetCharacter.HumanoidRootPart
            end
        end
    end
    return OldIndex(Self, Key)
end)

setreadonly(RawMetatable, true)

-- اضافه کردن Toggle به تب PVP مطابق داکیومنت
local AimbotToggle = PvpTab:Toggle({
    Title = "Aimbot (Guns)",
    Value = false,
    Callback = function(State)
        IsAimbotEnabled = State
        
        -- نمایش نوتیفیکیشن هنگام تغییر وضعیت
        WindUI:Notify({
            Title = "PVP System",
            Content = State and "ایم‌بات فعال شد" or "ایم‌بات غیرفعال شد",
            Type = "Info"
        })
    end
})

-- جداکننده و توضیحات محلی
local InfoDivider = PvpTab:Divider()
local InfoText = PvpTab:Button({
    Title = "توضیح: هرجا شلیک کنید تیر به نزدیک‌ترین پلیر می‌خورد",
    Callback = function() end
})
