-- تعریف سرویس‌های اصلی (Local)
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

-- بارگذاری کتابخانه WindUI
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

-- متغیرهای کنترلی اسکریپت
local lp = Players.LocalPlayer
local SelectedPlayerName = ""
local IsOrbiting = false
local FastAttackEnabled = false

-- مدیریت فیزیک و اتصالات
local currentOrbitConnection = nil
local currentOrbitBv = nil

-- ۱. ایجاد پنجره اصلی (Local)
local Window = WindUI:CreateWindow({
    Title  = "Vitex Hub - Anti-Seat V3",
    Icon   = "zap",
    Author = "by Vitex",
    Theme  = "Violet",
    Acrylic = true,
})

-- ۲. ایجاد تب اصلی (Local)
local MainTab = Window:Tab({
    Title = "Combat & Speed",
    Icon = "swords",
})

-- تابع محلی برای گرفتن لیست پلیرها
local function GetPlayerList()
    local list = {}
    local allPlayers = Players:GetPlayers()
    for i = 1, #allPlayers do
        local p = allPlayers[i]
        if p ~= lp then table.insert(list, p.Name) end
    end
    return list
end

-- ۳. ایجاد دراپ‌دان (Local)
local PlayerDrop = MainTab:Dropdown({
    Title = "انتخاب هدف",
    Multi = false,
    Options = GetPlayerList(),
    Callback = function(v) SelectedPlayerName = v end
})

-- ۴. ایجاد دکمه رفرش لیست (Local)
local RefreshBtn = MainTab:Button({
    Title = "بروزرسانی لیست پلیرها",
    Callback = function() 
        local newList = GetPlayerList()
        PlayerDrop:Refresh(newList) 
    end
})

-- تابع پاکسازی فیزیک و حرکت
local function StopMovement()
    if currentOrbitConnection then 
        currentOrbitConnection:Disconnect() 
        currentOrbitConnection = nil 
    end
    if currentOrbitBv then 
        currentOrbitBv:Destroy() 
        currentOrbitBv = nil 
    end
    local char = lp.Character
    if char then
        local des = char:GetDescendants()
        for i = 1, #des do
            local p = des[i]
            if p:IsA("BasePart") then p.CanCollide = true end
        end
    end
end

-- ۵. تاگل تعقیب فوق سریع (Anti-Seat & Speed 250)
local OrbitToggle = MainTab:Toggle({
    Title = "تعقیب چسبیده (Speed 250)",
    Value = false,
    Callback = function(state)
        IsOrbiting = state
        if not state then 
            StopMovement() 
            return 
        end
        
        local angle = 0
        currentOrbitConnection = RunService.Stepped:Connect(function(_, deltaTime)
            local target = Players:FindFirstChild(SelectedPlayerName)
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local tRoot = target and target.Character and target.Character:FindFirstChild("HumanoidRootPart")
            local tHead = target and target.Character and target.Character:FindFirstChild("Head")
            local myRoot = char and char:FindFirstChild("HumanoidRootPart")

            if tRoot and tHead and myRoot and hum then
                -- [بخش آنتی-سیت] - اگه نشست، بلندش کن
                if hum.Sit then 
                    hum.Sit = false 
                    hum.Jump = true 
                end

                -- Noclip فعال
                local parts = char:GetDescendants()
                for i = 1, #parts do
                    local p = parts[i]
                    if p:IsA("BasePart") then p.CanCollide = false end
                end

                -- ایجاد حالت بی‌وزنی
                if not currentOrbitBv or currentOrbitBv.Parent ~= myRoot then
                    if currentOrbitBv then currentOrbitBv:Destroy() end
                    local bv = Instance.new("BodyVelocity")
                    bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
                    bv.Velocity = Vector3.new(0, 0, 0)
                    bv.Parent = myRoot
                    currentOrbitBv = bv
                end

                -- محاسبات سرعت ۲۵۰ و موقعیت سر
                angle = angle + (deltaTime * 12)
                local radius = 4
                local heightOffset = 0.5 
                
                local offset = Vector3.new(math.cos(angle) * radius, heightOffset, math.sin(angle) * radius)
                local targetPos = tRoot.Position + offset
                
                -- تلپورت با ضریب ۰.۹
                myRoot.CFrame = myRoot.CFrame:Lerp(CFrame.new(targetPos, tHead.Position), 0.9)
            else
                StopMovement()
            end
        end)
    end
})

-- ۶. تاگل حمله خودکار داینامیک (Local)
local AttackToggle = MainTab:Toggle({
    Title = "حمله خودکار (Dynamic AOE)",
    Value = false,
    Callback = function(state)
        FastAttackEnabled = state
        if state then
            task.spawn(function()
                local function findRemote(parent, keyword)
                    if not parent then return nil end
                    local des = parent:GetDescendants()
                    for i = 1, #des do
                        local v = des[i]
                        if (v:IsA("RemoteEvent") or v:IsA("RemoteFunction")) and v.Name:lower():find(keyword:lower()) then
                            return v
                        end
                    end
                    return nil
                end

                local function getRemotes()
                    local net = ReplicatedStorage:FindFirstChild("Modules") and ReplicatedStorage.Modules:FindFirstChild("Net")
                    local atk = findRemote(net, "RegisterAttack")
                    local hit = findRemote(net, "RegisterHit")
                    local rFolder = ReplicatedStorage:FindFirstChild("Remotes")
                    local r874 = rFolder and rFolder:FindFirstChildOfClass("RemoteEvent")
                    return atk, hit, r874
                end

                local lastRef = 0
                local rAtk, rHit, r874

                while FastAttackEnabled do
                    pcall(function()
                        if tick() - lastRef > 5 then
                            rAtk, rHit, r874 = getRemotes()
                            lastRef = tick()
                        end

                        local root = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                        if root and lp.Character:FindFirstChildOfClass("Tool") then
                            if rAtk then rAtk:FireServer(0.4) end

                            local function Damage(targetModel)
                                local tRoot = targetModel:FindFirstChild("HumanoidRootPart") or targetModel:FindFirstChild("Head")
                                local hum = targetModel:FindFirstChild("Humanoid")
                                if tRoot and hum and hum.Health > 0 and (tRoot.Position - root.Position).Magnitude < 70 then
                                    if rHit then rHit:FireServer(tRoot, {}, nil, "694ba46d") end
                                    if r874 then r874:FireServer("VA+VacmwpavLmp", 13064117, tRoot, {}, "869bada7") end
                                end
                            end

                            local enemies = workspace:FindFirstChild("Enemies")
                            if enemies then
                                local eList = enemies:GetChildren()
                                for i = 1, #eList do Damage(eList[i]) end
                            end

                            local allP = Players:GetPlayers()
                            for i = 1, #allP do
                                local p = allP[i]
                                if p ~= lp and p.Character then Damage(p.Character) end
                            end
                        end
                    end)
                    task.wait(0.1)
                end
            end)
        end
    end
})
